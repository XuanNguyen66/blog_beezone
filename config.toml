[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true       # Hiện số dòng để dễ thảo luận kỹ thuật
    style = "monokai"    # Hoặc "dracula" cho phong cách chuyên nghiệp
    noClasses = true